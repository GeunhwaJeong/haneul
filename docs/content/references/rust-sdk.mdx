---
title: Legacy Rust SDK
description: >-
  The Haneul Rust SDK provides Rust wrappers around the Haneul API. Using the SDK, you
  can interact with Haneul networks using the Rust programming language.
keywords:
  - rust
  - rust SDK
  - haneul rust SDK
  - crates/haneul-sdk
  - haneul-sdk
goal:
  description: >-
    Reader can look up how to use the Haneul Rust SDK to interact with Haneul networks
    in Rust
  requires:
    - has_frontmatter:
        - title
        - description
      label: Has required frontmatter fields
    - min_words: 100
      label: Needs more reference content
    - has_questions: true
      label: Needs questions for AI search visibility
    - has_answer: true
      label: Needs answer summary for AI citation
questions:
  - How do I use the Legacy Rust SDK?
  - What is Legacy Rust SDK in Haneul?
  - What methods does the Legacy Rust SDK provide?
answer: >-
  The Legacy Rust SDK crate is in the [`crates/haneul-sdk`
  directory](https://github.com/GeunhwaJeong/haneul/tree/main/crates/haneul-sdk) of the
  Haneul repository.
---

The Legacy Rust SDK crate is in the [`crates/haneul-sdk` directory](https://github.com/GeunhwaJeong/haneul/tree/main/crates/haneul-sdk) of the Haneul repository.

This SDK is forward and backwards compatible with the [`haneul-rust-sdk`](https://github.com/GeunhwaJeong/haneul-rust-sdk) and supports JSON RPC. If you do not need JSON RPC, use the [`haneul-rust-sdk`](https://github.com/GeunhwaJeong/haneul-rust-sdk) instead.

<ImportContent source="crates/haneul-sdk/README.md" mode="code" style="md" />
