---
title: Haneul Framework Reference
description: >-
  The Haneul Framework includes the essential libraries necessary for developing
  Move packages.
keywords:
  - haneul framework
  - libraries
  - onchain libraries
  - haneul framework reference
goal:
  description: >-
    Reader can browse the Haneul framework module reference to find function
    signatures and type definitions
  requires:
    - has_frontmatter:
        - title
        - description
      label: Has required frontmatter fields
    - min_words: 100
      label: Needs more reference content
    - has_questions: true
      label: Needs questions for AI search visibility
    - has_answer: true
      label: Needs answer summary for AI citation
questions:
  - Where can I find the Haneul framework API reference?
  - What modules are available in the Haneul framework?
  - How do I browse the Haneul Move standard library?
answer: >-
  The Haneul Framework includes the essential libraries necessary for developing
  Move packages.
---

The Haneul Framework includes the core onchain libraries for Move developers. These libraries provide the types and functions you use to define objects, manage ownership, handle coins, emit events, and interact with system-level features.

## Browse the reference

Access the framework reference in either of the following formats:

- [Haneul Framework Reference docs](https://docs.haneul.io/references/framework/) — Searchable HTML reference generated from source. Use the search bar to find specific functions, structs, or modules.
- [Haneul GitHub repo](https://github.com/GeunhwaJeong/haneul/tree/main/crates/haneul-framework/docs) — Markdown format in the repository. Use GitHub search or browse the directory structure.

## Framework packages

The framework is organized into 3 packages:

### `haneul-framework`

Core types and functions for Haneul development. Commonly used modules include:

| **Module** | **Purpose** |
|---|---|
| `haneul::object` | Object creation (`new`), deletion (`delete`), and ID utilities |
| `haneul::transfer` | Transfer, share, and freeze objects across ownership types |
| `haneul::coin` | Fungible token standard (`Coin<T>`, `TreasuryCap`, minting, burning) |
| `haneul::event` | Emit events for offchain indexing and monitoring |
| `haneul::clock` | Access the network clock for timestamp-based logic |
| `haneul::dynamic_field` | Attach and manage dynamic fields on objects |
| `haneul::dynamic_object_field` | Attach and manage dynamic object fields |
| `haneul::display` | Object Display V2 template engine for offchain rendering |
| `haneul::package` | Package upgrade capabilities and policies |
| `haneul::tx_context` | Transaction metadata (sender, epoch, object creation) |

### `haneul-system`

Validator set, staking, and governance modules. You typically do not import these directly unless you are building staking interfaces or validator tooling.

### `move-stdlib`

The standard Move library provides foundational types available to all Move programs:

| **Module** | **Purpose** |
|---|---|
| `std::vector` | Growable, ordered collections |
| `std::option` | Optional values (`Option<T>`) |
| `std::string` | UTF-8 encoded strings |
| `std::hash` | SHA2-256 and SHA3-256 hash functions |
| `std::type_name` | Type reflection and name utilities |
| `std::ascii` | ASCII string handling |

## Find a specific function or type

To find a specific function or type in the framework:

1. Open the [HTML reference](https://docs.haneul.io/references/framework/).
2. Use the search bar to enter the function or type name (for example, `share_object`, `Coin`, `TreasuryCap`).
3. Click the result to see the full signature, type parameters, and documentation.
